<%= form_for(@tv_category) do |f| %>
  <% if @tv_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tv_category.errors.count, "error") %> prohibited this tv_category from being saved:</h2>

      <ul>
      <% @tv_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br>
    <%= f.text_field :category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
